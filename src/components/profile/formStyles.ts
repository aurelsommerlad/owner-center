// Shared classNames for the Profil page's forms - same rounded-xl/border-line
// input chrome the admin area introduced, so a first-time form on the Owner
// Center side still reads as the same design language rather than a new one.
export const PROFILE_INPUT_CLASS =
  "w-full rounded-xl border border-line bg-paper px-3.5 py-2.5 text-sm text-ink outline-none transition-colors placeholder:text-ink-soft/60 focus:border-ink";

export const PROFILE_LABEL_CLASS = "text-[11px] uppercase tracking-[0.08em] text-ink-soft";
